- 所有扫描器必须继承 `scanners/base_scanner.BaseScanner` 并实现 `scan` 方法，统一返回 `Finding` 数据类列表。
- 配置文件中敏感信息（如 API Key）采用 `${ENV_VAR}` 占位符格式，在运行时从环境变量动态解析。
- AI 分析结果以字典形式注入 `Finding.ai_analysis` 字段，保持原始扫描数据与增强数据的分离。
- 报告生成器采用统一的 `generate` 接口，根据配置自动输出多种格式（JSON/Markdown）并返回文件路径映射。