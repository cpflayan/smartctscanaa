# 智能合约安全审计报告

> 生成时间: 2026-06-26 04:26:10
> 生成工具: contract-vuln-detector

## 合约信息

| 项目 | 值 |
|------|-----|
| source | `local_file` |
| file_path | `/mnt/usb-jm/haha/contract-vuln-detector/examples/VulnerableBank.sol` |
| contract_name | `VulnerableBank` |

## 安全摘要

**整体风险等级**: UNKNOWN

> AI analysis was skipped. Review findings manually.

### 漏洞分布

| 严重程度 | 数量 |
|----------|------|
| 🔴 CRITICAL | 0 |
| 🟠 HIGH | 3 |
| 🟡 MEDIUM | 16 |
| 🟢 LOW | 4 |
| 🔵 INFO | 0 |

## 详细分析

### 1. 🟠 [HIGH] selfdestruct

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 59 行
- **函数**: `destroy()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.725

**工具描述**: 使用了 selfdestruct，可能导致合约资金被强制销毁且无法恢复。

**代码片段**:
```solidity
     56 | 
     57 |     // Vulnerability: selfdestruct without proper access control
     58 |     function destroy() public {
>>   59 |         selfdestruct(payable(msg.sender));
     60 |     }
     61 | 
     62 |     // Vulnerability: hardcoded address
```

---

### 2. 🟠 [HIGH] tx-origin-auth

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 35 行
- **函数**: `withdrawAll()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.718

**工具描述**: 使用 tx.origin 进行身份验证，存在钓鱼攻击风险。应改用 msg.sender。

**代码片段**:
```solidity
     32 | 
     33 |     // Vulnerability: tx.origin authentication
     34 |     function withdrawAll() public {
>>   35 |         require(tx.origin == owner, "Not owner");
     36 |         payable(owner).transfer(address(this).balance);
     37 |     }
     38 | 
```

---

### 3. 🟠 [HIGH] controlled-delegatecall

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 51 行
- **函数**: `upgradeTo()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.710

**工具描述**: 使用了 delegatecall，如果目标地址可控则存在代码注入风险。

**代码片段**:
```solidity
     48 | 
     49 |     // Vulnerability: delegatecall to user-controlled address
     50 |     function upgradeTo(address newImpl) public {
>>   51 |         (bool success, ) = newImpl.delegatecall(
     52 |             abi.encodeWithSignature("upgrade()")
     53 |         );
     54 |         require(success);
```

---

### 4. 🟡 [MEDIUM] unchecked-send

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 41 行
- **函数**: `refundUser()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.537

**工具描述**: .send() 调用未检查返回值，失败时不会 revert。应检查返回值或使用 .transfer()。

**代码片段**:
```solidity
     38 | 
     39 |     // Vulnerability: unchecked send
     40 |     function refundUser(address user, uint256 amount) public {
>>   41 |         balances[user].send(amount);
     42 |     }
     43 | 
     44 |     // Vulnerability: block.timestamp dependence
```

---

### 5. 🟡 [MEDIUM] reentrancy-risk

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 26 行
- **函数**: `withdraw()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.515

**工具描述**: 外部 .call{value: ...} 调用，如状态更新在调用之后则存在重入风险。

**代码片段**:
```solidity
     23 |         require(balances[msg.sender] >= amount, "Insufficient balance");
     24 | 
     25 |         // External call BEFORE state update = reentrancy risk
>>   26 |         (bool success, ) = msg.sender.call{value: amount}("");
     27 |         require(success, "Transfer failed");
     28 | 
     29 |         // State update AFTER external call
```

---

### 6. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 12 行
- **函数**: `initialize()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
      9 |     uint256 public totalDeposits;
     10 | 
     11 |     // Vulnerability: no access control on constructor-like init
>>   12 |     function initialize() public {
     13 |         owner = msg.sender;
     14 |     }
     15 | 
```

---

### 7. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 16 行
- **函数**: `deposit()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     13 |         owner = msg.sender;
     14 |     }
     15 | 
>>   16 |     function deposit() public payable {
     17 |         balances[msg.sender] += msg.value;
     18 |         totalDeposits += msg.value;
     19 |     }
```

---

### 8. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 22 行
- **函数**: `withdraw()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     19 |     }
     20 | 
     21 |     // Vulnerability: Reentrancy - state update after external call
>>   22 |     function withdraw(uint256 amount) public {
     23 |         require(balances[msg.sender] >= amount, "Insufficient balance");
     24 | 
     25 |         // External call BEFORE state update = reentrancy risk
```

---

### 9. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 34 行
- **函数**: `withdrawAll()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     31 |     }
     32 | 
     33 |     // Vulnerability: tx.origin authentication
>>   34 |     function withdrawAll() public {
     35 |         require(tx.origin == owner, "Not owner");
     36 |         payable(owner).transfer(address(this).balance);
     37 |     }
```

---

### 10. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 40 行
- **函数**: `refundUser()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     37 |     }
     38 | 
     39 |     // Vulnerability: unchecked send
>>   40 |     function refundUser(address user, uint256 amount) public {
     41 |         balances[user].send(amount);
     42 |     }
     43 | 
```

---

### 11. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 45 行
- **函数**: `isLucky()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     42 |     }
     43 | 
     44 |     // Vulnerability: block.timestamp dependence
>>   45 |     function isLucky() public view returns (bool) {
     46 |         return block.timestamp % 2 == 0;
     47 |     }
     48 | 
```

---

### 12. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 50 行
- **函数**: `upgradeTo()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     47 |     }
     48 | 
     49 |     // Vulnerability: delegatecall to user-controlled address
>>   50 |     function upgradeTo(address newImpl) public {
     51 |         (bool success, ) = newImpl.delegatecall(
     52 |             abi.encodeWithSignature("upgrade()")
     53 |         );
```

---

### 13. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 58 行
- **函数**: `destroy()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     55 |     }
     56 | 
     57 |     // Vulnerability: selfdestruct without proper access control
>>   58 |     function destroy() public {
     59 |         selfdestruct(payable(msg.sender));
     60 |     }
     61 | 
```

---

### 14. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 63 行
- **函数**: `setOracle()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     60 |     }
     61 | 
     62 |     // Vulnerability: hardcoded address
>>   63 |     function setOracle() public {
     64 |         address oracle = 0xdAC17F958D2ee523a2206206994597C13D831ec7;
     65 |     }
     66 | 
```

---

### 15. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 68 行
- **函数**: `transferOwnership()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     65 |     }
     66 | 
     67 |     // Vulnerability: no zero address check
>>   68 |     function transferOwnership(address newOwner) public {
     69 |         require(msg.sender == owner, "Not owner");
     70 |         owner = newOwner;
     71 |     }
```

---

### 16. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 74 行
- **函数**: `hashData()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     71 |     }
     72 | 
     73 |     // Vulnerability: sha3 deprecated
>>   74 |     function hashData(bytes memory data) public pure returns (bytes32) {
     75 |         return sha3(data);
     76 |     }
     77 | 
```

---

### 17. 🟡 [MEDIUM] missing-access-control

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 79 行
- **函数**: `addBalance()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.507

**工具描述**: external/public 函数缺少常见访问控制修饰符，需人工确认权限管理是否完善。

**代码片段**:
```solidity
     76 |     }
     77 | 
     78 |     // Vulnerability: integer overflow (pre-0.8.0)
>>   79 |     function addBalance(uint256 a, uint256 b) public pure returns (uint256) {
     80 |         return a + b; // No overflow check in Solidity <0.8
     81 |     }
     82 | }
```

---

### 18. 🟡 [MEDIUM] state-shadowing

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 4 行
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.500

**工具描述**: 可能存在状态变量影子问题，局部变量与状态变量同名可能导致逻辑错误。

**代码片段**:
```solidity
      1 | // SPDX-License-Identifier: MIT
      2 | pragma solidity ^0.7.6;
      3 | 
>>    4 | // A deliberately vulnerable contract for testing the scanner
      5 | 
      6 | contract VulnerableBank {
      7 |     mapping(address => uint256) public balances;
      8 |     address public owner;
      9 |     uint256 public totalDeposits;
```

---

### 19. 🟡 [MEDIUM] deprecated-sha3

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 75 行
- **函数**: `hashData()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.422

**工具描述**: sha3() 已弃用，应改用 keccak256()。

**代码片段**:
```solidity
     72 | 
     73 |     // Vulnerability: sha3 deprecated
     74 |     function hashData(bytes memory data) public pure returns (bytes32) {
>>   75 |         return sha3(data);
     76 |     }
     77 | 
     78 |     // Vulnerability: integer overflow (pre-0.8.0)
```

---

### 20. 🟢 [LOW] old-solidity-version

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 2 行
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.378

**工具描述**: 使用 Solidity 0.8.0 之前版本，整数运算未内置溢出检查。

**代码片段**:
```solidity
      1 | // SPDX-License-Identifier: MIT
>>    2 | pragma solidity ^0.7.6;
      3 | 
      4 | // A deliberately vulnerable contract for testing the scanner
      5 | 
```

---

### 21. 🟢 [LOW] timestamp-dependence

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 46 行
- **函数**: `isLucky()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.362

**工具描述**: 依赖 block.timestamp，矿工可在一定范围内操纵时间戳。

**代码片段**:
```solidity
     43 | 
     44 |     // Vulnerability: block.timestamp dependence
     45 |     function isLucky() public view returns (bool) {
>>   46 |         return block.timestamp % 2 == 0;
     47 |     }
     48 | 
     49 |     // Vulnerability: delegatecall to user-controlled address
```

---

### 22. 🟢 [LOW] transfer-risk

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 36 行
- **函数**: `withdrawAll()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.355

**工具描述**: .transfer() 有 2300 gas 限制，在接收方为合约时可能失败。

**代码片段**:
```solidity
     33 |     // Vulnerability: tx.origin authentication
     34 |     function withdrawAll() public {
     35 |         require(tx.origin == owner, "Not owner");
>>   36 |         payable(owner).transfer(address(this).balance);
     37 |     }
     38 | 
     39 |     // Vulnerability: unchecked send
```

---

### 23. 🟢 [LOW] hardcoded-address

- **状态**: ❓ 待确认
- **位置**: `examples/VulnerableBank.sol` 第 64 行
- **函数**: `setOracle()`
- **合约**: `for`
- **扫描工具**: pattern
- **综合评分**: 0.310

**工具描述**: 发现硬编码地址，建议使用常量或构造函数参数，便于部署到不同网络。

**代码片段**:
```solidity
     61 | 
     62 |     // Vulnerability: hardcoded address
     63 |     function setOracle() public {
>>   64 |         address oracle = 0xdAC17F958D2ee523a2206206994597C13D831ec7;
     65 |     }
     66 | 
     67 |     // Vulnerability: no zero address check
```

---

---
*报告由 contract-vuln-detector AI 自动生成，仅供参考。建议进行专业人工审计。*